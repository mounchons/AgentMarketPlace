# คู่มือ: Plan Mode → Long-Running Agent Workflow

## 📋 ภาพรวม

เอกสารนี้อธิบายขั้นตอนการใช้งาน Long-Running Agent plugin หลังจากใช้ Plan Mode ของ Claude Code

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PLAN MODE → LONG-RUNNING AGENT                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────┐      ┌─────────────┐      ┌─────────────────┐    │
│   │  PLAN MODE  │ ───▶ │ INIT-AGENT  │ ───▶ │    CONTINUE     │    │
│   │ (ออกแบบ/สอบถาม) │      │  (ครั้งเดียว) │      │  (ทำซ้ำหลายรอบ) │    │
│   └─────────────┘      └─────────────┘      └─────────────────┘    │
│                                                                     │
│   Output:               Output:               Output:              │
│   • แผนการพัฒนา          • feature_list.json  • Feature ที่เสร็จ     │
│   • Requirements         • .agent/            • Git commits        │
│   • Architecture         • progress.md        • Updated progress   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 เมื่อไหร่ควรใช้ Workflow นี้?

| สถานการณ์ | เหมาะสม? |
|-----------|---------|
| โปรเจคที่ต้องทำหลาย sessions | ✅ ใช่ |
| งานที่ซับซ้อน ต้องวางแผนก่อน | ✅ ใช่ |
| ต้องการ feature tracking | ✅ ใช่ |
| งานเล็กๆ ทำเสร็จใน 1 session | ❌ ไม่จำเป็น |
| Bug fix ง่ายๆ | ❌ ไม่จำเป็น |

---

## 🚀 ขั้นตอนการใช้งาน

### **Phase 1: Plan Mode (ออกแบบระบบ)**

Claude Code มี Plan Mode ในตัว ซึ่งจะเปิดใช้งานอัตโนมัติเมื่อ:
- งานมีความซับซ้อน
- ต้องตัดสินใจหลายอย่าง
- ผู้ใช้ขอให้ออกแบบก่อน

**วิธีเข้า Plan Mode:**

```bash
# ใน Claude Code CLI
> ช่วยออกแบบระบบ Todo API ด้วย .NET Core

# หรือระบุชัดเจน
> /plan ออกแบบระบบ HR Management
```

**สิ่งที่เกิดขึ้นใน Plan Mode:**

1. Claude สำรวจ codebase (ถ้ามี)
2. สอบถาม requirements จากผู้ใช้
3. ออกแบบ architecture
4. สร้างแผนการพัฒนา
5. เขียนแผนลงไฟล์ (เช่น `.claude/plan.md`)
6. ขอ approval จากผู้ใช้
7. ใช้ `ExitPlanMode` เมื่อพร้อม implement

**ผลลัพธ์จาก Plan Mode:**

```
project-root/
└── .claude/
    └── plan.md          # แผนการพัฒนาที่ได้รับ approval
```

**ตัวอย่างเนื้อหาใน plan.md:**

```markdown
# Todo API Implementation Plan

## Technology Stack
- ASP.NET Core 8.0 Web API
- Entity Framework Core
- PostgreSQL

## Features to Implement
1. Project Setup & Configuration
2. TodoItem Entity & DbContext
3. CRUD Endpoints
4. Input Validation
5. Error Handling
6. Swagger Documentation
...
```

---

### **Phase 2: Initialize Long-Running Agent**

หลังจาก approve แผนใน Plan Mode แล้ว ให้เริ่ม Long-Running Agent:

**Option 1: ใช้ Slash Command (แนะนำ)**

```bash
/init-agent สร้าง Todo API ตามแผนที่ออกแบบไว้
```

**Option 2: พิมพ์คำสั่งเอง**

```
สร้าง Long-Running Agent environment จากแผนที่ออกแบบไว้
- อ่านแผนจาก .claude/plan.md
- แปลงเป็น feature_list.json
- สร้าง .agent/ folder
```

**Option 3: สำหรับโปรเจคที่มีอยู่แล้ว**

```bash
/init-agent-existing
```

---

**สิ่งที่ Initializer Agent ทำ:**

```
┌─────────────────────────────────────────────────────────────────────┐
│  INITIALIZER AGENT WORKFLOW                                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1️⃣ อ่านเอกสารสำคัญ                                                 │
│     ├── CLAUDE.md (ถ้ามี)                                          │
│     ├── .claude/plan.md (แผนจาก Plan Mode)                        │
│     └── README.md                                                  │
│                                                                     │
│  2️⃣ วิเคราะห์ Requirements                                          │
│     ├── Project type (API, Web App, CLI)                           │
│     ├── Technology stack                                           │
│     └── Scope และ features                                         │
│                                                                     │
│  3️⃣ สร้าง Feature List                                              │
│     ├── แตก requirements เป็น features เล็กๆ (10-20 features)       │
│     ├── แต่ละ feature ทำเสร็จใน 1 session (15-30 นาที)              │
│     ├── เรียงตาม dependency (setup ก่อน)                           │
│     └── ทุก feature ต้อง passes: false                             │
│                                                                     │
│  4️⃣ สร้างไฟล์                                                       │
│     ├── .agent/config.json                                         │
│     ├── .agent/progress.md                                         │
│     └── feature_list.json                                          │
│                                                                     │
│  5️⃣ Git Commit                                                      │
│     └── "chore: Initialize long-running agent environment"         │
│                                                                     │
│  ❌ ห้าม implement code ใน phase นี้!                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**ไฟล์ที่ถูกสร้าง:**

```
project-root/
├── .agent/
│   ├── config.json        # Agent settings
│   └── progress.md        # Session logs
├── feature_list.json      # รายการ features (passes: false ทุกอัน)
└── init.sh               # Setup script (optional)
```

**ตัวอย่าง feature_list.json:**

```json
{
  "project": "Todo API",
  "description": "RESTful API สำหรับจัดการ Todo items",
  "created_at": "2025-01-15T10:00:00Z",
  "features": [
    {
      "id": 1,
      "category": "setup",
      "description": "สร้าง project structure และ dependencies",
      "priority": "high",
      "steps": [
        "dotnet new webapi -n TodoApi",
        "เพิ่ม EF Core packages",
        "ตั้งค่า appsettings.json"
      ],
      "passes": false,
      "tested_at": null,
      "notes": ""
    },
    {
      "id": 2,
      "category": "domain",
      "description": "สร้าง TodoItem entity และ DbContext",
      "priority": "high",
      "steps": [
        "สร้าง Models/TodoItem.cs",
        "สร้าง Data/AppDbContext.cs",
        "ตั้งค่า connection string"
      ],
      "passes": false,
      "tested_at": null,
      "notes": ""
    }
  ],
  "summary": {
    "total": 10,
    "passed": 0,
    "failed": 10,
    "last_updated": "2025-01-15T10:00:00Z"
  }
}
```

**ตัวอย่าง .agent/config.json:**

```json
{
  "project_name": "Todo API",
  "technology": "ASP.NET Core 8.0",
  "initialized_at": "2025-01-15T10:00:00Z",
  "current_session": 1,
  "settings": {
    "auto_commit": true,
    "require_tests": true,
    "max_features_per_session": 1
  }
}
```

---

### **Phase 3: Coding Sessions (ทำซ้ำจนเสร็จ)**

ทุกครั้งที่เริ่ม session ใหม่:

```bash
/continue
```

หรือพิมพ์เอง:

```
ทำหน้าที่ Coding Agent - อ่าน progress, ทำ feature ถัดไป
```

---

**Coding Agent Workflow:**

```
┌─────────────────────────────────────────────────────────────────────┐
│  CODING SESSION WORKFLOW                                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Step 0: อ่านเอกสารสำคัญ (ทุกครั้ง!)                                 │
│  ────────────────────────────────────                               │
│  cat CLAUDE.md 2>/dev/null                                         │
│  cat .agent/project-rules.md 2>/dev/null                           │
│                                                                     │
│  Step 1: Get Context (ต้องทำก่อนเสมอ!)                               │
│  ─────────────────────────────────────                              │
│  pwd                                                               │
│  cat .agent/progress.md                                            │
│  git log --oneline -10                                             │
│  cat feature_list.json                                             │
│                                                                     │
│  Step 2: Verify Environment                                         │
│  ──────────────────────────                                        │
│  dotnet build   # หรือ npm run build                               │
│  ถ้า build fail → แก้ไขก่อนทำ feature ใหม่                          │
│                                                                     │
│  Step 3: Select Feature                                             │
│  ──────────────────────                                            │
│  - หา feature ที่ passes=false                                     │
│  - เลือก priority=high ก่อน                                        │
│  - ทำทีละ 1 feature เท่านั้น!                                      │
│                                                                     │
│  Step 4: Implement Feature                                          │
│  ─────────────────────────                                         │
│  - ทำตาม steps ที่ระบุ                                             │
│  - เขียน code ที่ clean                                            │
│  - Handle errors appropriately                                     │
│                                                                     │
│  Step 5: Test Feature (สำคัญมาก!)                                   │
│  ────────────────────────────────                                  │
│  - Build ผ่าน                                                      │
│  - Manual test ผ่าน (curl, Postman, browser)                       │
│  - Edge cases handled                                              │
│  ❌ ห้าม mark pass โดยไม่ test!                                    │
│                                                                     │
│  Step 6: Mark as Passed                                             │
│  ──────────────────────                                            │
│  แก้ไข feature_list.json:                                          │
│  - passes: true                                                    │
│  - tested_at: "timestamp"                                          │
│  - notes: "test results"                                           │
│                                                                     │
│  Step 7: Commit Changes                                             │
│  ──────────────────────                                            │
│  git add .                                                         │
│  git commit -m "feat: Feature #X - description"                    │
│                                                                     │
│  Step 8: Update Progress Log                                        │
│  ───────────────────────────                                       │
│  เพิ่ม session ใหม่ใน .agent/progress.md                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**ตัวอย่าง Session Output:**

```
🎯 Working on Feature #3: GET /api/todos endpoint

Step 1: สร้าง TodoController
- สร้างไฟล์ Controllers/TodoController.cs
- เพิ่ม [ApiController] และ [Route("api/todos")]

Step 2: Implement GET endpoint
- Inject AppDbContext
- สร้าง GetAll() method

Step 3: Testing
- curl http://localhost:5000/api/todos → ✅ 200 OK
- curl http://localhost:5000/api/todos/1 → ✅ 200 OK
- curl http://localhost:5000/api/todos/999 → ✅ 404 Not Found

✅ Feature #3 PASSED

📝 Committing changes...
git commit -m "feat: Feature #3 - GET /api/todos endpoint"

📊 Progress: 3/10 features passed

📋 Next feature: #4 - POST /api/todos endpoint
```

---

## 📝 สรุป Commands ทั้งหมด

| Command | คำอธิบาย | ใช้เมื่อไหร่ |
|---------|----------|-------------|
| `/init-agent [description]` | Initialize agent environment | ครั้งแรกเท่านั้น (หลัง Plan Mode) |
| `/init-agent-existing` | Initialize สำหรับโปรเจคที่มีอยู่ | มี codebase อยู่แล้ว |
| `/continue` | ทำ feature ถัดไป | ทุกครั้งหลัง init |
| `/agent-status` | ดูความคืบหน้า | ต้องการดูสถานะ |
| `/add-feature [description]` | เพิ่ม feature ใหม่ | พบว่าต้องทำเพิ่ม |

---

## 💡 ตัวอย่าง Workflow แบบเต็ม

```
┌─────────────────────────────────────────────────────────────────────┐
│  EXAMPLE: สร้าง Todo API ด้วย ASP.NET Core                          │
└─────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════
Session 1: Plan Mode
════════════════════════════════════════════════════════════════════════

User:
> ช่วยออกแบบระบบ Todo API ด้วย ASP.NET Core และ PostgreSQL

Claude: [เข้า Plan Mode]

        ก่อนออกแบบ ขอสอบถามเพิ่มเติม:

        1. ต้องการ authentication ไหม?
        2. ต้องการ real-time updates (SignalR) ไหม?
        3. ต้องการ pagination สำหรับ list ไหม?

User:
> 1. ไม่ต้อง auth ก่อน
> 2. ไม่ต้อง real-time
> 3. ต้องการ pagination

Claude: [สร้างแผน]

        ## Proposed Architecture
        - ASP.NET Core 8.0 Web API
        - Entity Framework Core + PostgreSQL
        - Repository Pattern

        ## Features (10 items)
        1. Project Setup
        2. Domain Models
        3. GET endpoints (with pagination)
        4. POST endpoint
        5. PUT endpoint
        6. DELETE endpoint
        7. Input Validation
        8. Error Handling
        9. Swagger Docs
        10. Integration Tests

        [เขียนแผนลง .claude/plan.md]
        [ExitPlanMode]

════════════════════════════════════════════════════════════════════════
Session 2: Initialize Agent
════════════════════════════════════════════════════════════════════════

User:
> /init-agent สร้าง Todo API ตามแผน

Claude: [Initializer Agent]

        🚀 Initializing Long-Running Agent...

        1. ✅ อ่านแผนจาก .claude/plan.md
        2. ✅ สร้าง feature_list.json (10 features)
        3. ✅ สร้าง .agent/config.json
        4. ✅ สร้าง .agent/progress.md
        5. ✅ git commit "chore: Initialize agent"

        📁 Files created:
        ├── .agent/
        │   ├── config.json
        │   └── progress.md
        └── feature_list.json

        📊 Features: 0/10 passed

        ✅ พร้อมใช้ /continue เพื่อเริ่มทำ Feature #1

════════════════════════════════════════════════════════════════════════
Session 3: Feature #1
════════════════════════════════════════════════════════════════════════

User:
> /continue

Claude: [Coding Agent]

        📖 Reading context...
        - Last session: Initializer completed
        - Git log: 1 commit

        🎯 Feature #1: Project Setup

        Step 1: Create project
        $ dotnet new webapi -n TodoApi

        Step 2: Add packages
        $ dotnet add package Npgsql.EntityFrameworkCore.PostgreSQL
        $ dotnet add package Microsoft.EntityFrameworkCore.Design

        Step 3: Configure appsettings.json
        [editing file...]

        Step 4: Test build
        $ dotnet build
        Build succeeded. ✅

        ✅ Feature #1 PASSED

        📝 git commit -m "feat: Feature #1 - Project setup"
        📝 Updated .agent/progress.md

        📊 Progress: 1/10 features
        📋 Next: Feature #2 - Domain Models

════════════════════════════════════════════════════════════════════════
Session 4-12: Features #2-10
════════════════════════════════════════════════════════════════════════

User:
> /continue

[ทำซ้ำจนครบทุก feature...]

════════════════════════════════════════════════════════════════════════
Session 12: Final Feature
════════════════════════════════════════════════════════════════════════

Claude: [Coding Agent]

        🎯 Feature #10: Integration Tests

        [implement tests...]

        $ dotnet test
        Passed! 15/15 tests ✅

        ✅ Feature #10 PASSED

        ════════════════════════════════════════════
        🎉 PROJECT COMPLETED!
        ════════════════════════════════════════════

        📊 Final Status:
        - Features: 10/10 passed ✅
        - All tests: ✅
        - Build: ✅

        📋 Git commits: 12 total
        📁 Files created: 25 files

        ขอแสดงความยินดี! โปรเจคเสร็จสมบูรณ์แล้ว
```

---

## ⚠️ กฎสำคัญ

### สำหรับ Initializer Agent

| ❌ ห้ามทำ | ✅ ต้องทำ |
|----------|----------|
| Implement code จริง | สร้างแค่ configuration files |
| สร้าง source files | Feature list ต้องครบถ้วน |
| ทำ feature ใดๆ | Commit ทุกอย่าง |

### สำหรับ Coding Agent

| ❌ ห้ามทำ | ✅ ต้องทำ |
|----------|----------|
| One-shot (ทำทุกอย่างครั้งเดียว) | ทำทีละ 1 feature |
| Mark pass โดยไม่ test | Test จริงก่อน mark |
| ลบ/แก้ feature descriptions | แก้ได้แค่ `passes` และ `notes` |
| ประกาศเสร็จก่อนเวลา | ตรวจสอบว่าทุก feature pass |
| ทิ้ง code ที่มี bug | Build ต้องผ่านก่อนจบ session |

### กฎทั่วไป

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CRITICAL RULES                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. อ่าน progress ก่อนเริ่มงานเสมอ                                   │
│  2. ทำทีละ 1 feature เท่านั้น                                       │
│  3. Test ก่อน mark pass                                            │
│  4. Commit แยกต่าง feature                                         │
│  5. Update progress ก่อนจบ session                                 │
│  6. ทิ้ง code ในสถานะพร้อมใช้งาน                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 การแก้ไขปัญหา

### ถ้าไม่พบ feature_list.json

```bash
# ต้องรัน init ก่อน
/init-agent [description]
```

### ถ้า build fail

1. อ่าน error message
2. แก้ไข code
3. Build ใหม่
4. **อย่าทำ feature ใหม่จนกว่า build จะผ่าน**

### ถ้า test fail

1. วิเคราะห์ว่าผิดตรงไหน
2. แก้ไข code
3. Test ใหม่
4. **อย่า mark pass จนกว่าจะ test ผ่านจริง**

### ถ้า feature ซับซ้อนเกินไป

1. บันทึกความคืบหน้าที่ทำได้ใน `notes`
2. Commit code ที่มี
3. เขียนว่าติดตรงไหน
4. ให้ session ถัดไปทำต่อ

---

## 📚 เอกสารที่เกี่ยวข้อง

| ไฟล์ | คำอธิบาย |
|------|----------|
| `INITIALIZER_PROMPT.md` | Prompt สำหรับ Initializer Agent |
| `CODING_AGENT_PROMPT.md` | Prompt สำหรับ Coding Agent |
| `Manual.md` | คู่มือเริ่มต้นใช้งาน |
| `SIMULATION_EXAMPLE.md` | ตัวอย่างการทำงานจริง |

---

## 🔗 อ้างอิง

- [Effective harnesses for long-running agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) - Anthropic Engineering Blog
- [Claude Code Documentation](https://docs.anthropic.com/claude-code)
- [Claude 4 Prompting Guide](https://docs.claude.com/en/docs/build-with-claude/prompt-engineering/claude-4-best-practices)
